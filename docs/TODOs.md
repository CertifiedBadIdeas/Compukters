Заметки для дельных фич:

1. Глубокая интеграция с модом Create.
   1. Сделать возможность управлять механизмами Create через компьютер. Например, можно будет написать программу, которая будет включать и выключать механизмы, контролировать их работу и т.д. Это может быть полезно для автоматизации производства и создания сложных механизмов.
   2. Сделать возможность снимать информацию о состоянии механизмов Create через компьютер. Например, можно будет узнать, сколько ресурсов находится в определённом механизме, какой у него статус и т.д. Это может быть полезно для мониторинга производства и оптимизации работы механизмов.
   3. Сделать возможность получать информацию с основных блоков (латунных? или свои) Create. Например, можно будет узнать, сколько ресурсов находится в определённом блоке, какой у него статус и т.д. Это может быть полезно для мониторинга производства и оптимизации работы блоков. Так же можно добавить блок, который позволит смотреть сколько через конвеер пролетает предметов в секунду (пропускная способность) либо встроить это в туннели. Возможно сделать какие-то отдельные блоки Create, которые смогут прямо на компьютер отправлять информацию о своём состоянии. Хотя тоже надо бы подумать как это вообще реализовать, именно как реализовать систему такую. Может дать возможность присваивать уникальный ID? тогда надо будет подумать над многопользовательским режиом где у каждого будет куча таких блоков-передатчиков. Может быть сделать как в Create(как складская сеть, только передатчиками являются уже блоки типа туннелей, шлюзов, желобов, наблюдателей. Сделать крафт аля взять оригинальный блок + спец передатчик = блок-передатчик). Типо сеть таких устройств. Сделать контроллер такой сети. к которому уже подключается компьютер. В контроллере уже настраиваются биндинги и можно посмотреть что подключено и предварительно посмотреть информацию о блоке, которую он может передавать. Получится по сути система "умный дом", как в реальной жизни на протоколе zigbee. Ну и разумеется у контроллера будет (пропускная способность|кол-во максимальных подключенных устройств|дальность соединения) У самих устройств будет свой уникальный UUID, а контроллер по сути будет их собирать и их оркестрировать.
3. На основе import можно определять, можно ли вообще запустить программу на этом компьютере, возможно сделать какие-то запросы capability, peripheral, по imports по сути можно однозначно понять от чего программа зависит. Зависеть она может от инвентаря например, который есть только у черепашки.
3. Сделать черепашку со своим набором builtins - У черепашки появляется Fuel и Inventory. Можно сделать флаг конечно, черепашка это или нет, но даже не знаю надо ли
4. Сделать расширить систему import, добавив peripheral, возможно какой-то обозреватель import чтобы можно было узнать вообще какие peripheral вообще доступны. Так же сделать систему import файлов скриптов, чтобы можно было на них ссылаться, и тогда по сути проверка файла на возможность запуска будет ещё дополнена рекурсивной проверкой всех dependency файлов
5. Внешняя и внутренняя сеть между компьютерами, внутренняя сеть по сути своей должна представлять систему broadcast channels, с id канала(не компьютера, компьютеры не должны иметь возможность сообщатся просто напрямую)
6. git cli клиент
7. Сделать связь между компьютерами.
    1. Сделать модемы - радиомодули или лазерные(если в космос например хахахаха)
    2. Для модемов должна быть вышка, которая обслуживает эти модемы и обеспечивает связь между компьютерами.
8. Сделать workbench(компьютерный стол) где можно будет программировать компьютеры.
    1. Мне не очень нравится идея IDE прямо в компьютере. Кажется это сильно казуально.
    2. По сути будет спец обьект - компьютерный стол, где можно будет писать программу на конкретный компьютер.
    3. Для начала сделать ручную загрузку, но потом можно будет сделать апгрейд на стол, позволяющий на лету загружать что-то на компьютер и получать доступ к файловой системе, если у компьютера установлен модем беспроводного интернета и есть спец вышка, обслуживающая модем.
    4. Разработка на workbench должна быть по сути чисто клиентская с возможностью отправки/загрузки файлов на компьютер или полностью клонировать workspace себе. Делается для того, чтобы разработка не замедлялась из-за качества подключения к серверу.
    5. Подумать над возможностью запускать программы компьютера из IDE. Потому что будет неудобно писать, не имея возможности запускать код. Может либо через слот в инвентаре IDE или надо поставить комп рядом.

9. Сделать систему дебага, которая будет работать по сути через модемы. На компьютере должен быть установлен специальный дебагер, который будет отправлять информацию о состоянии программы на IDE, а IDE будет отображать эту информацию в виде логов и переменных. Это позволит отлаживать программы на компьютере, не имея доступа к его экрану.


## Workbench IDE (отложено после переписывания UI на DSL)

- **Clipboard API для CodeEditor.** Сейчас Ctrl+X/C/V в редакторе кода не работают — модель `EditorViewModel` поддерживает `selection`, но клавиатурные сочетания не подключены. Для полноценной IDE надо вынести clipboard-операции в отдельный интерфейс (доступ к системному буферу через Minecraft API) и обработать их в `CodeEditor` поверх текущей логики.
- **Shift+Arrow и выделение текста.** `EditorViewModel.selection: SelectionRange?` уже зарезервировано, но никем не выставляется. Нужно: расширить `WorkbenchStore` действиями `extendSelection*`, отрисовать прямоугольник выделения в `CodeEditor` (внутри ScrollArea), сделать `Backspace`/`Delete`/`Tab`/printable consume selection.