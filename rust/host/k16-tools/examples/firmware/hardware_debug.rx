use std::computer::debug_base;

fn main() -> i32 {
    let mut base: u32 = debug_base();
    if base == 0u32 {
        return 1;
    }

    unsafe {
        mmio<u8>(base).store(72u8);
        mmio<u8>(base).store(87u8);
    }

    return 0;
}
