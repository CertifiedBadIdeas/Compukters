use std::io::{read_byte, read_ready, write_byte, write_bytes};

fn main() {
    write_bytes(b"RUX READY\n", 10u32);

    while true {
        if read_ready() {
            write_byte(read_byte());
        }
    }
}
