pub fn control_base() -> u32 {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 1u32 {
                return ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    return 0u32;
}

pub fn debug_base() -> u32 {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 2u32 {
                return ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    return 0u32;
}

pub fn serial_input_base() -> u32 {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 3u32 {
                return ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    return 0u32;
}

pub fn write_status(status: i32) {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut base: u32 = 0u32;
    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 1u32 {
                base = ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    if base != 0u32 {
        unsafe {
            mmio<i32>(base).store(status);
        }
    }
}

pub fn set_booting() {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut base: u32 = 0u32;
    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 1u32 {
                base = ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    if base != 0u32 {
        unsafe {
            mmio<i32>(base).store(1);
        }
    }
}

pub fn set_ready() {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut base: u32 = 0u32;
    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 1u32 {
                base = ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    if base != 0u32 {
        unsafe {
            mmio<i32>(base).store(2);
        }
    }
}

pub fn set_panic(code: i32) {
    let mut table: u32 = 0u32;
    let mut count: u32 = 0u32;
    unsafe {
        table = ptr<u32>(RAM_BASE + 20).load();
        count = ptr<u32>(RAM_BASE + 24).load();
    }

    let mut base: u32 = 0u32;
    let mut i: u32 = 0u32;
    while i < count {
        let mut entry: u32 = table + i * 12u32;
        unsafe {
            if ptr<u32>(entry).load() == 1u32 {
                base = ptr<u32>(entry + 4).load();
            }
        }
        i += 1u32;
    }

    if base != 0u32 {
        unsafe {
            mmio<i32>(base + 4).store(code);
            mmio<i32>(base).store(4);
        }
    }
}
