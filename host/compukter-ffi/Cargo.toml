[package]
name = "compukter-ffi"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
compukter-vm = { path = "../compukter-vm" }

[dev-dependencies]
sha2 = "0.10"

[profile.release]
codegen-units = 1
lto = "thin"
